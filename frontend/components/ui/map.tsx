"use client";

/**
 * Map component has been replaced by GatewayMap (Google Maps).
 * Import GatewayMap from "@/components/ui/gateway-map" for gateway locations.
 */
export { GatewayMap as Map } from "./gateway-map";
